# 1Qubit State Viewer 仕様書

**バージョン:** MVP v0.1  
**最終更新:** 2025年  
**ステータス:** 設計完了・実装前

---

## 思想

これは「量子を勉強するアプリ」ではない。

「量子状態を触るアプリ」。

理解より先に、

> 「なんか普通の確率じゃない」

を感じられることを最優先にする。

---

## 最重要体験

このシーケンスを体験できることがMVPの完成条件。

```
Reset → H → Z → H → Measure
```

| ステップ | 画面上の変化 | 内部で起きていること |
|----------|-------------|---------------------|
| Reset | 確率 100:0 | alpha=1, beta=0 |
| H | 確率 50:50 | 重ね合わせ生成 |
| Z | 確率 変化なし | betaの符号が反転 |
| H | 確率 100:0 に戻る | 波の干渉が起きた |
| Measure | 状態が崩壊 | 確率で0か1に決定 |

**ポイント：** Zを押しても確率バーは変わらない。なのに次のHで結果が変わる。この「確率だけでは説明できない内部状態の存在」がこのアプリの核心。

---

## 実装範囲

### 実装するもの（MVP）

- 1キュービット
- Xゲート / Hゲート / Zゲート
- Measure / Reset
- ブロッホ球表示
- 波表示
- 確率バー

### 実装しないもの（MVP）

- 多キュービット・entanglement
- バックエンド・DB・保存機能
- ログイン
- 音変換・MIDI
- Qiskit等の本格量子計算ライブラリ

---

## 技術構成

### フロントエンド

| 項目 | 選定 | 理由 |
|------|------|------|
| ビルドツール | Vite | 高速・設定が少ない |
| UIフレームワーク | React | コンポーネント管理が容易 |
| 言語 | TypeScript | 型安全で状態管理が明確になる |
| 描画 | CSS / SVG / divアニメーション | Canvasは不要。後で移行可能 |

### ホスティング

**GitHub Pages**を使用。

- サーバー不要
- DB不要
- 追加料金不要
- pushするだけで自動デプロイ

公開URL形式：
```
https://{ユーザー名}.github.io/1qubit-viewer/
```

---

## 状態設計

### データ構造

```ts
type Complex = {
  re: number  // 実部
  im: number  // 虚部
}

type QubitState = {
  alpha: Complex  // |0⟩ の振幅
  beta: Complex   // |1⟩ の振幅
}
```

**なぜ複素数で持つか：** `phase` を別フィールドで管理すると、ゲートを重ねるたびに整合性の維持が難しくなる。複素数で持てば量子力学の本来の形そのままで計算でき、H→Z→H の干渉も自然に再現できる。

### 初期状態（|0⟩）

```ts
{
  alpha: { re: 1, im: 0 },
  beta:  { re: 0, im: 0 }
}
```

### 表示への変換

```ts
// 確率（確率バー用）
const prob0 = alpha.re ** 2 + alpha.im ** 2
const prob1 = beta.re  ** 2 + beta.im  ** 2

// 位相（波表示用）
const phase = Math.atan2(beta.im, beta.re)
```

---

## ゲート仕様

### Xゲート（量子版NOT）

```
|0⟩ ↔ |1⟩
```

```ts
alpha_new = beta
beta_new  = alpha
```

### Hゲート（アダマール）

重ね合わせの生成 / 干渉の実行

```ts
alpha_new = (alpha + beta) / √2
beta_new  = (alpha - beta) / √2
```

### Zゲート（位相反転）

確率は変化しない。内部状態のみ変化。

```ts
alpha_new = alpha
beta_new  = { re: -beta.re, im: -beta.im }
```

### Measure（観測）

確率に従い0か1へ崩壊させる。

```ts
const rand = Math.random()
if (rand < prob0) {
  // |0⟩ に崩壊
  alpha = { re: 1, im: 0 }, beta = { re: 0, im: 0 }
} else {
  // |1⟩ に崩壊
  alpha = { re: 0, im: 0 }, beta = { re: 1, im: 0 }
}
```

演出：
- 状態ベクトルが縮小
- 北極または南極へ吸い込まれる
- 確率バーが固定される

### Reset

初期状態 |0⟩ に戻す。

---

## UIレイアウト

```
+--------------------------------------+
| 波表示            | ブロッホ球       |
|                    |                  |
+--------------------------------------+
| X | H | Z | Measure | Reset         |
+--------------------------------------+
| 状態表示 / 確率バー                 |
+--------------------------------------+
```

---

## 各表示の仕様

### 確率バー

```
|0⟩ : 50% ████████░░
|1⟩ : 50% ████████░░
```

更新タイミング：ゲート操作のたびに更新。Measure後は固定。

### ブロッホ球

| 位置 | 意味 |
|------|------|
| 北極 | \|0⟩ |
| 南極 | \|1⟩ |
| 赤道 | 重ね合わせ状態 |

ゲートごとの挙動：

| ゲート | 状態ベクトルの動き |
|--------|------------------|
| H | 北極 → 赤道 （または赤道 → 北極/南極） |
| Z | 赤道上を180°回転 |
| Measure | 北極または南極へ収束 |

### 波表示

目的：ブロッホ球だけでは見えない「位相の変化」を視覚化する。

| 状態 | 波の見え方 |
|------|-----------|
| \|0⟩ | alpha波のみ |
| H後 | alpha波とbeta波が半々 |
| Z後 | beta波が反転 |
| H→Z→H後 | 干渉により波が収束 |

---

## 開発手順

### 環境構築

```bash
# Node.jsバージョン確認
node -v  # v20以上が望ましい

# プロジェクト作成
npm create vite@latest 1qubit-viewer -- --template react-ts
cd 1qubit-viewer
npm install

# 動作確認
npm run dev
# → http://localhost:5173 で確認
```

### GitHub Pages設定

**vite.config.ts に追記：**

```ts
export default defineConfig({
  base: '/1qubit-viewer/',
  plugins: [react()],
})
```

**.github/workflows/deploy.yml を作成：**  
pushのたびに自動でビルド＆デプロイされる。

### 実装順序

```
STEP 1  quantum.ts    状態管理・ゲート関数
STEP 2  ボタン        X / H / Z / Measure / Reset
STEP 3  確率バー      最もシンプルな表示・動作確認を兼ねる
STEP 4  ブロッホ球    空間的な視覚化
STEP 5  波表示        位相の視覚化
```

**重要：** UIより先に `quantum.ts` の計算が正しいことを確認する。H→Z→H で prob0 が 1.0 に戻れば計算は正しい。

### Claude Codeへの最初の指示

```
1キュービットの状態ビューアを作ります。
状態は Complex な alpha と beta で管理します。

まず STEP 1:
QubitStateの型定義と、
X / H / Z / Measure / Reset の関数を
src/quantum.ts に書いてください。
UIはまだ不要です。
```

---

## MVP完成条件

以下がすべて成立すること。

```
H を押す     → prob0 = 0.5, prob1 = 0.5
Z を押す     → prob0 = 0.5, prob1 = 0.5（変化なし）
H を押す     → prob0 = 1.0, prob1 = 0.0（干渉）
Measure      → |0⟩ に崩壊（100%なので確定）
```

---

## 今後の拡張計画

### v0.2

- Yゲート
- RX / RY / RZ（任意角度の回転）
- ブロッホ球をドラッグで操作
- 位相変化のアニメーション

### v0.3

- 2キュービット
- CNOTゲート
- entanglement（量子もつれ）の可視化

### 最終目標：8Qubit Music

| 要素 | 内容 |
|------|------|
| 状態ループ | 量子ゲートのシーケンスを繰り返す |
| 干渉による音変化 | 波の干渉結果を音程・音色に変換 |
| 観測による揺らぎ | Measureのたびに少し違う音が出る |
| 量子状態の保存 | 演奏状態をURLやファイルで共有 |
| 毎回少し違う演奏 | 量子的不確定性を音楽的表現に |

量子干渉 → 音の干渉、という発想。「理解して聴く」のではなく「体で感じる」という思想の延長線上にある。

---

## 重要な注意点

### 状態の正規化

ゲート適用後は必ず正規化する。

```ts
const norm = Math.sqrt(prob0 + prob1)
alpha = { re: alpha.re / norm, im: alpha.im / norm }
beta  = { re: beta.re  / norm, im: beta.im  / norm }
```

浮動小数点誤差が積み重なるとブロッホ球の表示がずれる原因になる。

### ユーザー導線

ボタンを並べるだけだと最重要シーケンスを発見できない可能性がある。チュートリアルではなく「ヒント」として一行添える。

```
← おすすめ: H → Z → H → Measure を試してみて
```
